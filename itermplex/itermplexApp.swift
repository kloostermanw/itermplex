import SwiftUI

@main
struct itermplexApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .preferredColorScheme(.dark)
        }
        .defaultSize(width: 300, height: 760)
    }
}
